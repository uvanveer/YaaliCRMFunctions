string standalone.Test_function9()
{
temp = toString(zoho.currenttime.subDay(90),"yyyy-MM-dd'T'HH:mm:ss") + "-05:00";
offset = "0";
data = "select Store,Store.Name,Dropship_Number,Order_Date,Contact_Name,Contact_Name.Full_Name,Subject,Subject1 from EDI_log where (((Created_Time >= '" + temp + "') and (Subject ='Not Resolved')) and (Test_dropship_order = false))  OFFSET " + offset + " LIMIT 200";
info " All quotes req: " + data;
coql_map = Map();
coql_map.put("select_query",data);
info coql_map;
// maillist = invokeurl
// [
// 	url :"https://www.zohoapis.com/crm/v2/coql"
// 	type :POST
// 	parameters:coql_map.tostring()
// 	connection:"crm_connection"
// ];
// info "maillist" + maillist;
return "";
}